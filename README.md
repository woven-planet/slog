# slog
Structured Logging

# Contributing
## Running tests
* `bazel test --test_output=errors src/...` -- run all unit tests.

## Code formatting
* `./lint.sh ./ -i` -- automatically format all code.
